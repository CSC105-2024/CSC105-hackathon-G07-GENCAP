export type examScore = {
    userId : number
    examId : number
    
}

export type addWord = {
    userId : number
    questionId : number
}

export type newExamScore = {
    isTrue : boolean
    userId : number
    examId : number
}